"
set nobackup "禁止生成备份文件
set noswapfile "禁止创建交换文件
set noundofile "禁用创建撤销文件
set nowritebackup "编辑的时候不需要备份文件

" 
syntax on 
syntax enable

"
"set nu " line number
"set relativenumber "相对行号
set mouse="" "取消左键可视化(visual)

" vim 缩进
set tabstop=4 "设置控制制表符所代表的空格数
set softtabstop=4 "Tab 键时光标移动的宽度
set shiftwidth=4 "控制缩进的宽度
set expandtab "表示缩进用空格来表示
set autoindent "自动缩进 
